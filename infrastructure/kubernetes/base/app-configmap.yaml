apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.appName }}-config"
  namespace: "{{ .Values.namespace }}"
  labels:
    app: "{{ .Values.appName }}"
data:
  API_URL: "{{ .Values.backend.apiUrl }}"
  REDIS_HOST: "{{ .Values.appName }}-redis"
  REDIS_PORT: "{{ .Values.redis.containerPort | toString }}"
  DATABASE_HOST: "{{ .Values.appName }}-database"
  DATABASE_PORT: "{{ .Values.database.containerPort | toString }}"
  DATABASE_NAME: "{{ .Values.database.name }}"
  DATABASE_USER: "{{ .Values.database.user }}"
  LOG_LEVEL: "{{ .Values.logLevel }}"
  ENVIRONMENT: "{{ .Values.environment }}"
  MAX_CONNECTIONS: "100"
  CONNECTION_TIMEOUT: "30"
